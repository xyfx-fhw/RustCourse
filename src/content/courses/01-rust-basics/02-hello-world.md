---
title: "Hello, World!"
description: "编写你的第一个 Rust 程序"
duration: 20
difficulty: "beginner"
tags: ["入门", "基础语法", "cargo"]
hasPlayground: true
defaultCode: |
  fn main() {
      println!("Hello, world!");
  }
---

# Hello, World!

## 速览

在编程世界中，"Hello, World!" 是学习新语言的传统第一步。在本课中，你将：
- 创建你的第一个 Rust 项目
- 了解 Rust 项目的基本结构
- 编译并运行 Rust 程序

## 详细讲解

### 使用 Cargo 创建新项目

Cargo 不仅是包管理器，还是 Rust 的构建工具。让我们用它创建第一个项目：

```bash
cargo new hello_rust
cd hello_rust
```

这会创建一个新的目录 `hello_rust`，包含以下结构：

```
hello_rust/
├── Cargo.toml
└── src/
    └── main.rs
```

### 项目文件说明

**Cargo.toml** - 项目配置文件：

```toml
[package]
name = "hello_rust"
version = "0.1.0"
edition = "2021"

[dependencies]
```

**src/main.rs** - 主程序文件：

```rust
fn main() {
    println!("Hello, world!");
}
```

### 代码解析

让我们逐行理解这段代码：

```rust
fn main() {
```

- `fn` 关键字用于定义函数
- `main` 是特殊的函数名，程序的入口点
- `()` 表示这个函数不接受参数

```rust
    println!("Hello, world!");
```

- `println!` 是一个**宏**(macro)，注意末尾的 `!`
- 用于将文本打印到控制台
- 字符串使用双引号 `""`
- 语句以分号 `;` 结尾

```rust
}
```

- 大括号 `{}` 包围函数体

### 编译和运行

#### 方法 1: 使用 Cargo (推荐)

```bash
cargo run
```

Cargo 会：
1. 编译你的代码
2. 生成可执行文件
3. 自动运行程序

你会看到输出：

```
   Compiling hello_rust v0.1.0 (/path/to/hello_rust)
    Finished dev [unoptimized + debuginfo] target(s) in 0.50s
     Running `target/debug/hello_rust`
Hello, world!
```

#### 方法 2: 使用 rustc

你也可以直接使用 Rust 编译器：

```bash
rustc src/main.rs
./main  # Linux/macOS
main.exe  # Windows
```

但对于真实项目，始终推荐使用 Cargo。

### 修改程序

现在让我们修改程序，使其更有趣：

```rust
fn main() {
    println!("Hello, Rust!");
    println!("欢迎来到 Rust 世界 🦀");

    // 这是注释
    println!("Rust 支持中文！");
}
```

再次运行：

```bash
cargo run
```

你会看到：

```
Hello, Rust!
欢迎来到 Rust 世界 🦀
Rust 支持中文！
```

## 实践练习

1. 创建一个新的 Rust 项目 `my_first_rust`
2. 修改 `main.rs`，打印你的名字和一条欢迎消息
3. 使用 `cargo run` 运行程序
4. 尝试添加多行输出

**挑战**: 尝试使用 `cargo build` 只编译程序（不运行），然后手动运行生成的可执行文件。

## 常见问题

### println! 为什么有感叹号？

感叹号 `!` 表示这是一个**宏**而不是普通函数。宏是 Rust 的强大特性，可以在编译时生成代码。

### Cargo.toml 是什么？

`Cargo.toml` 是 Rust 项目的配置文件，使用 TOML 格式。它包含：
- 项目元信息（名称、版本）
- Rust 版本 (edition)
- 依赖包列表

### target 目录是什么？

`target/` 目录包含编译生成的文件：
- `target/debug/` - 调试版本（未优化）
- `target/release/` - 发布版本（优化后）

## 要点总结

- ✅ 使用 `cargo new` 创建新项目
- ✅ `main` 函数是程序的入口点
- ✅ `println!` 宏用于打印输出
- ✅ `cargo run` 编译并运行程序
- ✅ Rust 完全支持 UTF-8 和 Emoji 😊

## 下一步

恭喜！你已经编写并运行了第一个 Rust 程序。在下一课中，我们将学习 Rust 的变量和数据类型。
